dist: trusty
addons:
  chrome: stable
language: java
jdk: oraclejdk8
sudo: required
cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.arquillian"

script:
- mvn clean test -Ptests -Darquillian.browser=chromeheadless
